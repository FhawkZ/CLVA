# Data Directory

This directory stores datasets and intermediate artifacts for CLVA:

- Raw trajectories (for example from MolmoSpaces).
- Processed chunk-level training samples.
- Metadata, split files, and cache files.

Use this folder as a placeholder now and add concrete dataset formats later.
